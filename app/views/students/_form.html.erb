<div class="form-card">
  <div class="form-title">
    🎓 <%= student.new_record? ? "Nowy student" : "Edycja studenta" %>
  </div>

  <%= form_with(model: student) do |f| %>

    <div class="form-group">
      <%= f.label :first_name, "Imię" %>
      <%= f.text_field :first_name, placeholder: "Jan" %>
    </div>

    <div class="form-group">
      <%= f.label :last_name, "Nazwisko" %>
      <%= f.text_field :last_name, placeholder: "Kowalski" %>
    </div>

    <div class="form-group">
      <%= f.label :album, "Numer albumu" %>
      <%= f.text_field :album, placeholder: "12345" %>
    </div>

    <div class="form-group">
      <%= f.label :group_id, "Grupa" %>
      <%= f.collection_select :group_id, Group.all, :id, :name, include_blank: "— Wybierz grupę —" %>
    </div>

    <div class="form-actions">
      <%= f.submit student.new_record? ? "Dodaj studenta" : "Zapisz zmiany", class: "btn-primary" %>
      <%= link_to "Anuluj", students_path, class: "btn-secondary" %>
    </div>

  <% end %>
</div>
